{
  "name": "orbit-concierge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker proxy that turns an Orbit profile into real plans.",
  "scripts": {
    "test": "node --test \"test/**/*.test.mjs\"",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.124.0"
  }
}
